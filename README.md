# Vite + Tauri + Svelte + RxJS + TypeScript

### In case of using Visual Studio Code

Extension: `Svelte for VS Code`

Settings:
  `"svelte.plugin.svelte.note-new-transformation": false`
  `"svelte.enable-ts-plugin": true`